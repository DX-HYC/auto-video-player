## 前置准备

在开始之前，请确保你满足以下条件：

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows / macOS / Linux 均可 |
| 浏览器 | Chrome 或 Edge（最新稳定版） |
| 项目文件 | 已下载并解压 `自动连播助手` 源码到本地目录 |
| 视频网站账号 | 可选（部分课程网站需登录才能观看） |

> **提示**：本项目是浏览器扩展，不需要安装 Node.js 或任何构建工具，源码即开即用。

---

## 5 分钟跑起来（最小可用示例）

### 第 1 步：打开扩展管理页面

- **Chrome**：在地址栏输入 `chrome://extensions` 并回车
- **Edge**：在地址栏输入 `edge://extensions` 并回车

### 第 2 步：开启「开发者模式」

找到页面右上角的 **「开发者模式」** 开关，点击将其打开。开启后，页面左上角会出现「加载已解压的扩展程序」按钮。

### 第 3 步：加载扩展

1. 点击 **「加载已解压的扩展程序」** 按钮
2. 在弹出的文件选择窗口中，**选择本项目源码所在的根目录**（即包含 `manifest.json` 文件的文件夹）
3. 点击「选择文件夹」确认

加载成功后，扩展列表中会出现「自动连播助手」的卡片。

### 第 4 步：验证效果

1. 打开任意支持的视频网站（如 Bilibili、YouTube、腾讯视频等）
2. 播放任意视频
3. 观察页面**右下角**——会出现一个深色悬浮面板，显示当前视频进度和状态

**验证功能**：

- **暂停自动续播**：点击视频暂停，等待 2 秒，视频会自动恢复播放
- **自动连播**：播放到视频结尾，扩展会自动点击「下一集」或播放列表中的下一个视频

> 如果页面右下角没有出现悬浮面板，请参考文末的「排错指南」。

---

## 进阶用法

### 一、视频暂停自动续播

**功能说明**：当视频因网络缓冲、手动暂停或其他原因停止播放时，扩展会在短暂延迟后自动点击播放按钮。

**默认行为**：
- 检测到视频暂停后，等待约 2 秒自动续播
- 若视频处于**手动暂停**状态（用户主动点击暂停），扩展会尊重用户操作，**不会**强制续播

**适用场景**：
- 网课平台视频因防挂机机制自动暂停
- 网络波动导致视频缓冲中断
- 视频播放器自身 bug 导致意外暂停

### 二、自动识别播放列表并穿透 iframe

**功能说明**：许多课程网站（如智慧树、超星学习通等）将视频嵌入 iframe 中，且课程目录在另一个 iframe 中。扩展会自动：

1. 穿透多层 iframe 找到实际播放视频的 `<video>` 元素
2. 在顶层页面中定位课程目录的 iframe
3. 解析目录结构，识别当前播放的是第几集

**无需任何配置**，打开课程页面后扩展会自动工作。

### 三、播完自动连播下一集（含漏看课时自动补齐）

**功能说明**：当当前视频播放完毕后，扩展会：

1. 自动点击「下一集」按钮（或播放列表中的下一个视频链接）
2. 如果检测到中间有**漏看的课时**（例如当前看到第 5 集，但第 3 集未完成），扩展会优先跳转到漏看的课时进行补齐

**判断逻辑**：
- 扩展根据课程目录中每集的「已完成」状态判断是否漏看
- 漏看课时会按顺序优先播放
- 全部课时完成后，会从头开始循环播放（可选）

### 四、实时显示视频进度与课程总进度

**功能说明**：悬浮面板会实时显示：

- **当前视频进度**：如 `12:34 / 45:00`，并带有进度条
- **课程总进度**：如 `3/12 集已完成`，并显示百分比

**面板信息示意**：

```
┌─────────────────────────────┐
│ ● 自动连播助手     [运行中] │
│                             │
│ 当前视频: 第 3 讲  ▓▓▓░░░  │
│ 进度: 12:34 / 45:00         │
│                             │
│ 课程总进度: 3/12 集 (25%)   │
│ ▓▓▓░░░░░░░░░░░░░░░░░░░░░░  │
│                             │
│ [暂停续播: 开] [日志] [折叠] │
└─────────────────────────────┘
```

### 五、悬浮控制面板操作

#### 拖拽移动

按住面板**顶部标题栏**（显示「自动连播助手」的区域），拖动到屏幕任意位置。

#### 折叠 / 展开

点击面板底部的 **「折叠」** 按钮（或面板右上角的 `—` 图标），面板会收缩为一个圆形小按钮。点击该按钮可重新展开。

#### 查看调试日志

点击面板底部的 **「日志」** 按钮，会展开一个日志区域，显示扩展的实时运行状态，包括：

- 检测到几个 `<video>` 元素
- 是否穿透了 iframe
- 当前播放列表解析结果
- 自动续播 / 连播的触发记录

**日志示例**：

```
[12:00:01] 检测到 1 个 video 元素
[12:00:01] 当前视频: 第 3 讲 (时长 45:00)
[12:00:05] 视频暂停，2 秒后尝试续播...
[12:00:07] 已自动恢复播放
[12:00:45] 视频播放结束，查找下一集...
[12:00:45] 发现漏看课时: 第 2 讲 (进度 30%)
[12:00:45] 正在跳转到: 第 2 讲
```

#### 暂停 / 恢复自动续播

面板底部有一个 **「暂停续播」** 开关按钮，点击可临时关闭自动续播功能（例如你确实想暂停视频做笔记时）。再次点击恢复。

### 六、快捷键控制

| 快捷键 | 功能 |
|--------|------|
| `Alt + N` | 全局切换自动连播的开关（开 / 关） |

> 快捷键在视频页面内生效。切换时会显示悬浮提示。

---

## 排错指南

### 现象 1：页面右下角没有出现悬浮面板

**可能原因与解决方案**：

| 原因 | 解决方案 |
|------|----------|
| 当前网站不在支持列表中 | 打开一个已知支持的网站测试（如 `bilibili.com`） |
| 扩展未正确加载 | 回到 `chrome://extensions`，确认扩展卡片显示「已启用」；若显示错误，点击「重新加载」 |
| 页面在 iframe 中 | 部分嵌套页面需刷新顶层页面；按 `F12` 打开开发者工具，在 Console 中查看是否有扩展日志输出 |
| 浏览器缓存了旧页面 | 强制刷新页面：`Ctrl + Shift + R`（Windows）或 `Cmd + Shift + R`（Mac） |

### 现象 2：视频暂停后没有自动续播

**排查步骤**：

1. 确认悬浮面板上的 **「暂停续播」** 开关为「开」状态
2. 查看面板日志，确认是否有「检测到视频暂停」的记录
3. 如果日志显示「手动暂停，不自动续播」，说明扩展检测到这是用户主动暂停——请先点击视频播放，再暂停，观察是否触发自动续播
4. 检查该网站是否有自定义播放器（非 HTML5 `<video>`），部分 Flash 播放器无法被识别

### 现象 3：播放结束后没有自动连播下一集

**排查步骤**：

1. 确认当前视频确实**播放到了最后**（进度 100%）
2. 查看日志，确认是否有「视频播放结束，查找下一集」的记录
3. 如果日志显示「未找到播放列表」，说明扩展无法解析该网站的目录结构。可尝试：
   - 刷新页面，等待 3-5 秒让目录加载完成
   - 手动点击一次目录中的「下一集」，让扩展学习播放列表结构
4. 如果日志显示「未找到漏看课时，且无下一集」，说明当前已是最后一集，扩展不会重复播放（除非开启循环模式）

### 现象 4：扩展在其他无关网页上也生效

**说明**：扩展内置了域名白名单机制，仅对已知的视频/课程网站生效。如果遇到非白名单网站也出现悬浮面板，可能是该页面包含 `<video>` 元素且域名规则匹配。

**解决方案**：在非视频页面，直接点击悬浮面板底部的「暂停续播」开关，或使用 `Alt + N` 快捷键关闭自动连播。

### 现象 5：视频进度显示不准确

**可能原因**：

- 视频有多个分段（如片头、正片、片尾），扩展统计的是整个 `<video>` 元素的播放进度
- 页面使用了自定义进度条，与原生 `<video>` 进度不同步

**解决方案**：以视频播放器自带的进度显示为准，扩展的进度百分比仅作为参考。

### 现象 6：扩展加载时报错

**错误示例**：

```
Failed to load extension
Manifest file is missing or unreadable
```

**解决方案**：

1. 确认选择的目录是**项目根目录**（该目录下应直接包含 `manifest.json` 文件，而不是嵌套的子目录）
2. 确认 `manifest.json` 文件没有被修改或损坏——重新从源码仓库下载一份对比
3. 确认浏览器版本支持 Manifest V3（Chrome 88+ / Edge 88+）

---

## 进阶阅读

| 资源 | 说明 |
|------|------|
| [项目 README](../../README.md) | 项目完整介绍、功能列表、技术架构 |
| [CHANGELOG.md](../../CHANGELOG.md) | 版本更新历史与功能变更记录 |
| [源码 `content.js`](../../src/content.js) | 核心逻辑实现（视频检测、自动续播、连播调度） |
| [源码 `content.css`](../../src/content.css) | 悬浮面板样式定义（可自定义主题） |
| [Manifest V3 官方文档](https://developer.chrome.com/docs/extensions/develop/concepts/manifest-v3) | Chrome 扩展开发规范（了解权限、生命周期等） |

---

> **提示**：本项目为 MIT 开源协议，欢迎提交 Issue 或 Pull Request 贡献代码。如果遇到文档中未覆盖的问题，请在 GitHub 仓库中搜索相似问题或新建 Issue。