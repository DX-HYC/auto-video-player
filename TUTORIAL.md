## 前置准备

在开始之前，请确认你的环境满足以下条件：

| 项目 | 要求 |
|------|------|
| 浏览器 | Chrome 88+ 或 Edge 88+（基于 Chromium 内核） |
| 操作系统 | Windows / macOS / Linux 均可 |
| 技术基础 | 无需编程经验，但需要能解压 ZIP 文件 |
| 视频网站 | 已安装扩展后，支持常见视频/网课平台（见文末列表） |

### 1. 获取项目文件

从项目仓库下载源码，有两种方式：

**方式 A：Git 克隆（推荐）**

```bash
git clone https://github.com/your-repo/auto-video-player.git
```

**方式 B：直接下载 ZIP**

1. 打开项目仓库页面
2. 点击绿色 `Code` 按钮 → 选择 `Download ZIP`
3. 解压到本地任意目录，例如 `~/Downloads/auto-video-player`

> ⚠️ **注意**：请记住解压后的文件夹路径，后续加载扩展时需要用到。

---

## 5 分钟跑起来（最小可用示例）

### 第 1 步：打开扩展管理页

**Chrome 用户**：在地址栏输入 `chrome://extensions` 并回车

**Edge 用户**：在地址栏输入 `edge://extensions` 并回车

### 第 2 步：开启开发者模式

在扩展管理页的**右上角**，找到「开发者模式」开关，点击将其**打开**。

开启后，页面顶部会出现一排操作按钮（加载已解压、打包扩展等）。

### 第 3 步：加载扩展

1. 点击页面左上角的 **「加载已解压的扩展程序」** 按钮
2. 在弹出的文件选择窗口中，**选中你解压的项目根目录**（即包含 `manifest.json` 文件的文件夹）
3. 点击「选择文件夹」

加载成功后，扩展列表中会出现「自动连播助手」的卡片。

### 第 4 步：验证是否生效

1. 打开任意支持的视频网站（如 B 站、优酷、中国大学 MOOC 等）
2. 播放任意一个视频
3. 观察页面**右下角**——出现一个深色悬浮面板，即表示扩展已生效

**面板初始状态说明：**

- 顶部显示「自动连播助手」和绿色「运行中」状态标签
- 面板内会实时显示视频进度百分比
- 如果页面有播放列表，会显示课程总进度

> 💡 **没看到面板？** 请先确认你访问的网站域名是否在支持列表中（见文末附录），或查看「排错指南」第 1 条。

---

## 进阶用法

### 一、视频暂停自动续播

**功能说明**：当视频因网络缓冲、手动暂停或其他原因停止播放时，扩展会在短暂延迟后自动点击播放按钮。

**默认行为**：
- 暂停超过 **2 秒** 后自动恢复播放
- 仅对页面中**正在播放**的视频生效（不会误触其他静默视频）

**操作步骤**：

1. 正常播放视频
2. 手动点击暂停，或等待视频因缓冲自动暂停
3. 观察——约 2 秒后，视频会自动恢复播放

**适用场景**：网课学习时切走聊天、视频缓冲卡顿后自动恢复。

---

### 二、播完自动连播下一集

**功能说明**：当当前视频播放结束后，扩展会自动识别播放列表中的下一集并点击播放。

**操作步骤**：

1. 打开一个包含多集视频的页面（如课程目录、剧集列表）
2. 播放任意一集
3. 等待当前视频播放至结束
4. 扩展会自动：
   - 识别页面中的播放列表结构
   - 定位当前集数
   - 自动点击下一集

**效果**：视频无缝衔接，无需手动操作。

---

### 三、漏看课时自动补齐

**功能说明**：如果播放列表中某几集因网络或其他原因被跳过（未完成播放），扩展会在当前集结束后自动回补播放漏看的课时。

**触发条件**：
- 播放列表中存在进度低于 95% 的课时
- 当前集已播放完毕

**行为逻辑**：

```
当前集播放结束
    ↓
检查列表中是否有未完成课时？
    ├─ 有 → 自动跳转到最早未完成的课时播放
    └─ 无 → 正常播放下一集
```

**操作步骤**：

1. 正常播放课程视频
2. 手动刷新页面或跳转到后面的集数（模拟漏看）
3. 继续播放至当前集结束
4. 观察扩展是否自动跳回漏看的课时

---

### 四、实时进度显示

**功能说明**：悬浮面板实时显示两个维度的进度：

| 进度类型 | 显示位置 | 含义 |
|---------|---------|------|
| 视频进度 | 面板中部 | 当前视频的播放百分比 |
| 课程总进度 | 面板底部 | 整个播放列表的完成百分比 |

**操作步骤**：

1. 确保面板处于展开状态（未被折叠）
2. 播放视频，观察面板数值变化
3. 切换集数，查看总进度是否同步更新

> 💡 **提示**：总进度 = 已完成的课时数 ÷ 总课时数 × 100%。单集播放超过 95% 即视为「已完成」。

---

### 五、悬浮面板操作

面板支持以下交互操作：

| 操作 | 方法 | 效果 |
|------|------|------|
| 拖拽移动 | 按住面板顶部标题栏拖动 | 改变面板位置 |
| 折叠/展开 | 点击面板右上角「—」按钮 | 收起为一个小圆球，再次点击展开 |
| 查看调试日志 | 点击面板底部「日志」按钮 | 展开日志区域，显示扩展运行状态 |

**操作步骤**：

1. **拖拽**：鼠标左键按住面板顶部（标题「自动连播助手」区域），拖动到屏幕任意位置
2. **折叠**：点击面板右上角的 `—` 图标，面板会收起为一个圆形小按钮；点击该按钮可重新展开
3. **查看日志**：点击面板底部的「日志」文字，面板内会展开一个滚动区域，显示最近的运行日志（如「检测到视频元素」「已自动续播」等）

> ⚠️ **注意**：日志功能主要用于调试，日常使用可保持关闭。

---

### 六、跨 iframe 视频识别

**功能说明**：部分网课平台将视频放在嵌套的 iframe 中，扩展能自动穿透 iframe 定位并控制视频。

**支持的场景**：
- 页面内嵌 iframe 播放器
- 多层嵌套 iframe
- Shadow DOM 中的视频元素

**操作步骤**：

1. 打开使用 iframe 嵌入视频的页面（如部分企业培训平台）
2. 正常播放视频
3. 扩展会自动注入到 iframe 内部，无需额外配置

**验证方法**：打开调试日志，观察是否出现 `[iframe] 检测到视频元素` 之类的日志信息。

---

## 排错指南

### 问题 1：打开视频网站后，右下角没有悬浮面板

**可能原因与解决方案**：

| 原因 | 排查方法 | 解决方案 |
|------|---------|---------|
| 网站不在支持列表 | 查看文末「支持平台列表」 | 更换支持的网站测试 |
| 扩展未正确加载 | 打开 `chrome://extensions`，确认卡片存在且无红色报错 | 移除后重新「加载已解压的扩展程序」 |
| 页面为顶层 frame 且非视频页 | 扩展设计上在非视频网站顶层不注入 | 打开具体视频播放页（而非首页） |
| 浏览器缓存问题 | 按 F12 打开控制台，查看是否有红色报错 | 刷新页面（Ctrl+Shift+R 强制刷新） |

### 问题 2：视频暂停后没有自动续播

**排查步骤**：

1. 确认面板状态标签为绿色「运行中」，而非灰色「已关闭」
2. 确认暂停时间超过 2 秒（扩展有防误触延迟）
3. 打开调试日志，查看是否有 `[暂停检测] 2s 后自动续播` 相关记录
4. 检查页面是否有多个视频元素——扩展只控制**当前正在播放**的那个

**如果日志无记录**：尝试刷新页面，重新触发视频加载。

### 问题 3：播放结束后没有自动连播下一集

**排查步骤**：

1. 确认页面**确实存在播放列表**（如课程目录、剧集列表），而非单视频页面
2. 打开调试日志，观察视频结束时的日志输出：
   - `[结束检测] 视频播放结束` → 说明结束事件已捕获
   - `[列表识别] 未找到播放列表` → 说明扩展未能识别列表结构
3. 如果列表未被识别，尝试：
   - 刷新页面，等待列表完全加载后再播放
   - 手动点击一次列表中的其他集数，帮助扩展定位列表容器

### 问题 4：面板显示异常或样式错乱

**解决方案**：

1. 刷新页面（F5）
2. 如果问题持续，移除扩展后重新加载：
   - 打开 `chrome://extensions`
   - 点击「自动连播助手」卡片上的「移除」
   - 重新点击「加载已解压的扩展程序」，选择项目目录

### 问题 5：扩展在某些网站上完全无效

**原因**：扩展默认只对已知视频平台注入脚本（见代码中的 `KNOWN_VIDEO_HOSTS` 列表），这是为了**避免在无关网页上产生副作用**。

**解决方案**：

1. 确认网站域名是否在支持列表中（见文末附录）
2. 如果不在列表中，但页面确实包含 `<video>` 元素：
   - 手动修改源码中的 `KNOWN_VIDEO_HOSTS` 数组，添加你的域名
   - 保存后，在 `chrome://extensions` 页面点击扩展卡片上的「重新加载」图标

---

## 进阶阅读

- **项目 README**：仓库根目录的 `README.md` 包含项目概述、功能列表和开发计划
- **CHANGELOG.md**：查看版本更新历史与功能迭代记录
- **源码结构**：
  - `content.js` — 核心逻辑（视频检测、自动播放、列表识别）
  - `content.css` — 悬浮面板样式
  - `manifest.json` — 扩展配置文件（权限声明、脚本注入规则）
- **二次开发建议**：如果你需要添加新的视频平台支持，修改 `content.js` 中的 `KNOWN_VIDEO_HOSTS` 数组即可

---

## 附录：支持平台列表

| 分类 | 平台 |
|------|------|
| 视频网站 | B站、优酷、爱奇艺、腾讯视频、YouTube、Vimeo、Twitch、抖音、快手、虎牙、斗鱼 |
| 网课平台 | 中国大学MOOC、网易云课堂、学堂在线、智慧树、慕课网、超星学习通、腾讯课堂、高顿、高小帮、多贝、学习强国 |
| 文档平台 | 道客巴巴、豆丁网（含视频内容时） |

> 不在列表中的网站，如果页面包含 `<video>` 元素，扩展在 iframe 中仍可能生效（因为 iframe 内的注入不受域名白名单限制）。