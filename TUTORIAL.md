# 自动连播助手 使用教程

---

## 前置准备

### 环境要求

| 项目 | 要求 |
|------|------|
| 浏览器 | Chrome 88+ 或 Edge 88+（基于 Chromium） |
| 操作系统 | Windows / macOS / Linux 均可 |
| 项目文件 | 已下载或克隆本项目源码到本地 |

### 获取项目文件

如果你还没有项目文件，通过以下方式获取：

```bash
git clone https://github.com/your-repo/auto-video-player.git
cd auto-video-player
```

> 如果无法使用 Git，也可以直接下载 ZIP 压缩包并解压到本地任意目录。

---

## 5 分钟跑起来（最小可用示例）

### 第 1 步：打开扩展管理页面

在浏览器地址栏输入以下地址之一并回车：

- Chrome：`chrome://extensions`
- Edge：`edge://extensions`

### 第 2 步：开启开发者模式

在扩展管理页面**右上角**找到「开发者模式」开关，点击将其打开。

### 第 3 步：加载扩展

1. 点击页面左上角的 **「加载已解压的扩展程序」** 按钮。
2. 在弹出的文件选择窗口中，**选中项目根目录**（即包含 `manifest.json` 文件的文件夹）。
3. 点击「选择文件夹」确认加载。

加载成功后，扩展列表中会出现「自动连播助手」的卡片。

### 第 4 步：验证生效

1. 打开任意支持的视频网站（如 Bilibili、YouTube、腾讯视频等）。
2. 播放任意视频。
3. 观察页面**右下角**——出现悬浮控制面板即表示扩展已生效。

> ✅ **验证成功标志**：面板显示当前视频进度百分比，且视频暂停后会在数秒内自动恢复播放。

---

## 进阶用法

### 一、自动续播（视频暂停自动恢复）

**功能说明**：当视频因网络缓冲、手动暂停或其他原因停止播放时，扩展会自动检测并在短暂延迟后恢复播放。

**使用方式**：

- 无需额外配置，默认开启。
- 若视频在播放中暂停超过 3 秒，扩展会自动点击播放按钮。

**自定义设置**：

1. 点击悬浮面板右上角的 **⚙️ 设置** 图标。
2. 找到「续播延迟」选项，可设置 0-10 秒的延迟时间。
3. 保存后立即生效。

---

### 二、自动连播（播完自动下一集）

**功能说明**：当前视频播放结束后，自动切换到播放列表中的下一集。

**使用方式**：

1. 打开包含播放列表的视频页面（如课程目录、剧集列表）。
2. 扩展会自动识别页面中的播放列表结构。
3. 当前视频播完后，自动点击下一集并开始播放。

**播放模式切换**：

点击悬浮面板上的模式按钮，在以下三种模式间循环切换：

| 模式 | 行为 |
|------|------|
| 🔄 顺序播放 | 按列表顺序播放，播完最后一集停止 |
| 🔁 列表循环 | 播完最后一集后回到第一集重新播放 |
| 🔀 随机播放 | 从列表中随机选择下一集 |

---

### 三、漏看课时自动补齐

**功能说明**：适用于网课/培训平台。如果检测到列表中某些课时未观看（进度低于阈值），扩展会优先播放未看课时。

**使用方式**：

1. 打开网课页面（如学堂在线、中国大学 MOOC、智慧树等）。
2. 扩展自动扫描课程目录，标记未看课时。
3. 当前课时结束后，自动跳转到**最早未看的课时**进行播放。

**查看补齐进度**：

- 悬浮面板会显示「课程总进度：XX%」。
- 点击面板上的 **📋 目录** 按钮，可展开查看每个课时的完成状态。

---

### 四、iframe / Shadow DOM 穿透识别

**功能说明**：部分视频网站将播放器嵌入在 iframe 或 Shadow DOM 中，扩展可自动穿透这些层级并识别视频元素。

**使用方式**：

- 无需手动操作，扩展自动检测。
- 若页面包含多层嵌套 iframe，扩展会逐层深入查找 `<video>` 标签。

**验证方法**：

1. 打开一个使用 iframe 嵌入视频的页面。
2. 点击悬浮面板上的 **🐞 调试日志** 按钮。
3. 查看日志中是否出现 `[穿透] 找到 iframe 层级: 2` 类似记录。

---

### 五、悬浮控制面板操作

#### 拖拽移动

- 鼠标左键按住面板**顶部标题区域**，拖动到屏幕任意位置。

#### 折叠/展开

- 点击面板右上角的 **「—」** 按钮折叠为一个小圆点。
- 点击小圆点重新展开面板。

#### 查看调试日志

1. 点击面板底部的 **「调试日志」** 展开按钮。
2. 日志面板显示最近 50 条运行记录，包括：
   - 视频元素发现记录
   - 播放/暂停状态变化
   - 列表切换动作
   - 错误信息

---

### 六、多标签页独立控制

**功能说明**：每个浏览器标签页中的扩展状态互不干扰。

**使用方式**：

- 在标签页 A 中暂停自动连播，不影响标签页 B 的自动连播。
- 每个标签页的面板独立显示该页面的视频进度。

---

## 排错指南

### 现象 1：加载扩展后，打开视频网站没有出现悬浮面板

**可能原因与解决思路**：

| 排查步骤 | 操作 |
|----------|------|
| 1. 检查站点是否支持 | 确认当前网站域名在支持列表中（Bilibili、YouTube、腾讯视频、学堂在线等）。若为小众网站，扩展默认不激活。 |
| 2. 检查扩展是否启用 | 打开 `chrome://extensions`，确认「自动连播助手」右侧开关为开启状态。 |
| 3. 刷新页面 | 扩展加载后，已打开的页面需要刷新一次才能注入脚本。按 `F5` 或 `Ctrl+R` 刷新。 |
| 4. 查看控制台报错 | 按 `F12` 打开开发者工具 → Console 标签，查看是否有红色报错信息。 |

---

### 现象 2：视频暂停后没有自动恢复播放

**可能原因与解决思路**：

| 排查步骤 | 操作 |
|----------|------|
| 1. 检查是否为手动暂停 | 扩展只自动恢复**非用户主动暂停**的情况（如网络卡顿）。若用户手动点击暂停，扩展不会干预。 |
| 2. 检查续播延迟设置 | 打开面板设置，确认「续播延迟」不为 0（0 表示禁用自动续播）。 |
| 3. 检查播放器类型 | 部分自定义播放器（非标准 HTML5）可能无法识别。查看调试日志是否有 `[错误] 未找到播放按钮` 记录。 |

---

### 现象 3：播完一集后没有自动切换到下一集

**可能原因与解决思路**：

| 排查步骤 | 操作 |
|----------|------|
| 1. 检查播放列表是否被识别 | 点击面板「目录」按钮，看是否列出了课程/剧集列表。若为空，说明扩展未能识别该网站的列表结构。 |
| 2. 检查播放模式 | 确认当前模式为「顺序播放」或「列表循环」，而非「随机播放」。 |
| 3. 检查是否为最后一集 | 顺序播放模式下，最后一集播完不会继续播放。 |
| 4. 手动点击下一集 | 如果列表识别失败，可尝试手动点击下一集，扩展会记录该行为并学习列表结构。 |

---

### 现象 4：面板显示但视频进度一直为 0%

**可能原因与解决思路**：

| 排查步骤 | 操作 |
|----------|------|
| 1. 检查视频是否真的在播放 | 确认页面中的视频已开始播放，而非处于加载状态。 |
| 2. 检查是否为多视频页面 | 如果页面包含多个 `<video>` 元素，扩展默认监控**当前正在播放**的那个。尝试暂停其他视频。 |
| 3. 查看调试日志 | 打开调试日志，查看是否有 `[信息] 正在监控视频 #2` 的记录。 |

---

### 现象 5：扩展在部分页面导致卡顿

**可能原因与解决思路**：

| 排查步骤 | 操作 |
|----------|------|
| 1. 关闭调试日志 | 调试日志会占用一定性能，日常使用建议关闭。 |
| 2. 折叠面板 | 面板的进度条实时刷新会消耗资源，折叠面板可降低开销。 |
| 3. 禁用不常用站点 | 打开扩展设置页，在「启用站点」列表中取消勾选不常访问的网站。 |

---

## 进阶阅读

| 资源 | 说明 | 链接 |
|------|------|------|
| README.md | 项目完整说明、功能列表、技术架构 | 项目根目录 `README.md` |
| CHANGELOG.md | 版本更新记录与变更日志 | 项目根目录 `CHANGELOG.md` |
| manifest.json | 扩展权限声明与配置说明 | 项目根目录 `manifest.json` |
| content.js | 核心逻辑源码（含详细注释） | `src/content.js` |
| 常见问题 FAQ | 更多用户问题解答 | 项目 Wiki 页面 |

---

> 💡 **提示**：如果以上排错指南未能解决你的问题，请在项目 GitHub 仓库提交 Issue，并附上调试日志截图，以便快速定位问题。