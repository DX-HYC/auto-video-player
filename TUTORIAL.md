# 自动连播助手 · 使用教程

---

## 一、前置准备

在开始之前，请确认你的环境满足以下条件：

| 项目 | 要求 |
|------|------|
| 浏览器 | Chrome（版本 88+）或 Edge（版本 88+） |
| 操作系统 | Windows / macOS / Linux 均可 |
| 项目文件 | 已下载并解压 `auto-video-player` 项目源码到本地目录 |
| 网络 | 无需特殊网络环境，扩展完全本地运行 |

> **提示**：本项目为浏览器扩展，不需要安装 Node.js 或任何依赖包。

---

## 二、5 分钟跑起来（最小可用示例）

### 第 1 步：打开扩展管理页面

**Chrome 用户**：在地址栏输入 `chrome://extensions` 并回车。

**Edge 用户**：在地址栏输入 `edge://extensions` 并回车。

### 第 2 步：开启「开发者模式」

在扩展管理页面的**右上角**（Chrome）或**左侧边栏底部**（Edge），找到「开发者模式」开关，点击将其**打开**。

开启后，页面顶部或左上角会出现一排操作按钮。

### 第 3 步：加载已解压的扩展程序

1. 点击页面左上角的 **「加载已解压的扩展程序」** 按钮（Chrome 为蓝色按钮，Edge 为白色带边框按钮）。
2. 在弹出的文件选择窗口中，**定位到你解压后的项目根目录**（即包含 `manifest.json` 文件的文件夹）。
3. 点击「选择文件夹」确认。

加载成功后，扩展列表中会出现 **「自动连播助手」** 的卡片，状态显示为「已启用」。

### 第 4 步：验证生效

1. 打开任意包含视频的网页（如 Bilibili、YouTube 或任意在线课程网站）。
2. 等待 1-2 秒，观察页面**右下角**。
3. 如果看到一个深色半透明的**悬浮控制面板**，说明扩展已成功注入页面。

> **验证小技巧**：随便打开一个视频开始播放，然后手动暂停。如果视频在约 1.2 秒后自动恢复播放，说明核心功能已正常工作。

---

## 三、进阶用法

### 3.1 自动续播（暂停后自动恢复）

**功能说明**：当视频因网络缓冲、手动暂停或其他原因停止播放时，扩展会自动点击播放按钮恢复播放。

**默认状态**：关闭（出于安全考虑，避免被视频平台误判为违规插件）。

**开启方法**：

1. 点击页面右下角的悬浮面板**头部区域**（标题栏），展开面板。
2. 找到 **「自动续播」** 开关。
3. 点击开关，使其变为**绿色/开启**状态。

**自定义恢复延迟**：

1. 在面板中点击 **「设置」** 图标（齿轮按钮）。
2. 找到「恢复延迟」输入框。
3. 输入你希望的等待毫秒数（默认 1200ms，建议范围 500-5000ms）。
4. 点击「保存」即可生效。

---

### 3.2 自动连播（播完自动下一集）

**功能说明**：当前视频播放结束后，扩展会自动识别页面中的播放列表（课程目录），并自动点击播放下一集。

**默认状态**：开启。

**工作流程**：

1. 扩展自动扫描页面中的 `<video>` 元素。
2. 视频播放结束后，扩展尝试定位播放列表容器。
3. 识别当前正在播放的集数，自动点击下一集。
4. 如果检测到中间有**漏看的课时**（例如从第 3 集跳到第 5 集），扩展会优先补齐播放第 4 集。

**关闭方法**：在面板中找到 **「自动连播」** 开关，点击使其变为灰色/关闭状态。

---

### 3.3 漏看课时自动补齐

**功能说明**：当播放列表中存在未观看的中间集数时，扩展会按顺序补齐播放，而不是直接跳到最新一集。

**适用场景**：在线课程平台（如中国大学 MOOC、网易云课堂等）。

**使用前提**：

- 播放列表结构清晰，每集有独立的链接或可点击元素。
- 扩展能正确识别当前视频在列表中的位置。

**操作步骤**：

1. 确保「自动连播」开关为开启状态。
2. 手动跳转到某一集（例如第 5 集）开始播放。
3. 当第 5 集播放结束后，扩展会自动检测到第 4 集尚未观看。
4. 扩展会先跳转到第 4 集播放，完成后继续第 5 集、第 6 集……依此类推。

> **注意**：补齐逻辑仅在同一播放列表内生效。如果页面没有可识别的播放列表结构，该功能自动降级为顺序连播。

---

### 3.4 实时进度显示

**功能说明**：悬浮面板实时显示当前视频的播放进度百分比，以及整个课程（播放列表）的总进度。

**查看方法**：

1. 观察悬浮面板主体区域。
2. 面板中会显示类似以下信息：
   - `当前视频：45%`（当前视频播放进度）
   - `课程总进度：12/30 集（40%）`（已完成集数 / 总集数）

**刷新时机**：进度信息每秒自动刷新一次，无需手动操作。

---

### 3.5 悬浮面板操作

#### 拖拽移动

1. 将鼠标移动到面板**头部区域**（标题栏）。
2. 按住鼠标左键不放。
3. 拖动到屏幕任意位置后松开鼠标。

#### 折叠 / 展开

- **折叠**：点击面板头部的 **「—」**（最小化）按钮，面板会收缩为一个圆形小按钮。
- **展开**：点击圆形小按钮，面板恢复完整显示。

#### 查看调试日志

1. 在面板中找到 **「日志」** 按钮（通常为终端图标或「Debug」字样）。
2. 点击后，面板会切换为日志视图，实时滚动显示扩展的运行日志。
3. 日志内容包括：视频元素扫描结果、播放状态变化、自动点击动作等。

**复制日志**：

1. 在日志视图底部点击 **「复制日志」** 按钮。
2. 日志内容会以纯文本格式复制到剪贴板，方便粘贴到 Issue 或反馈中。

**退出日志视图**：点击日志视图右上角的 **「返回」** 按钮。

---

### 3.6 设置面板

点击悬浮面板头部的 **齿轮图标**，可进入设置界面：

| 设置项 | 说明 | 默认值 |
|--------|------|--------|
| 自动续播 | 暂停后自动恢复播放 | 关闭 |
| 自动连播 | 播完自动播放下一集 | 开启 |
| 恢复延迟 | 暂停后等待多久恢复播放（毫秒） | 1200 |
| 调试模式 | 开启后输出详细日志 | 关闭 |

**保存设置**：修改后点击「保存」按钮，设置会持久化存储到浏览器本地（`chrome.storage`），刷新页面后依然生效。

---

## 四、排错指南

### 4.1 悬浮面板没有出现

| 可能原因 | 排查思路 | 解决方法 |
|----------|----------|----------|
| 扩展未正确加载 | 打开 `chrome://extensions`，确认「自动连播助手」卡片存在且开关为开启状态 | 重新加载扩展：点击卡片上的「刷新」图标 |
| 页面不支持 | 确认页面包含 `<video>` 元素 | 打开任意视频网站测试（如 Bilibili） |
| 扩展冲突 | 其他扩展可能干扰注入 | 尝试在无痕模式下加载扩展（需在扩展详情中开启「允许在无痕模式下运行」） |
| 页面缓存 | 旧版脚本未刷新 | 刷新页面（Ctrl+Shift+R 强制刷新） |

### 4.2 视频暂停后不自动续播

| 可能原因 | 排查思路 | 解决方法 |
|----------|----------|----------|
| 自动续播开关未开启 | 检查面板中「自动续播」是否为绿色开启状态 | 打开开关 |
| 恢复延迟设置过长 | 检查设置中的「恢复延迟」数值 | 调低至 1000ms 左右 |
| 网站播放器特殊 | 部分网站（如 YouTube）可能阻止自动播放 | 手动点击一次播放按钮，观察是否恢复自动续播 |
| 视频元素嵌套过深 | 视频位于多层 iframe 或 Shadow DOM 中 | 开启「调试模式」，查看日志中是否成功扫描到视频元素 |

### 4.3 播放结束不自动连播

| 可能原因 | 排查思路 | 解决方法 |
|----------|----------|----------|
| 自动连播开关未开启 | 检查面板中「自动连播」是否为绿色开启状态 | 打开开关 |
| 未识别到播放列表 | 开启调试模式，查看日志中是否有「播放列表容器」相关输出 | 如果无输出，说明页面结构不被支持，可尝试手动点击下一集后观察扩展是否学习 |
| 视频结束事件未捕获 | 部分自定义播放器可能不触发标准 `ended` 事件 | 在视频接近结束时手动拖动进度条到末尾，测试是否能触发连播 |
| 页面结构动态变化 | 播放列表是异步加载的 | 等待页面完全加载后再播放视频 |

### 4.4 进度显示不准确

| 可能原因 | 排查思路 | 解决方法 |
|----------|----------|----------|
| 多视频元素 | 页面存在多个 `<video>` 标签，扩展可能监控了错误的元素 | 开启调试模式，查看日志中「当前视频」指向哪个元素 |
| 播放列表识别错误 | 扩展可能将非课程列表的导航菜单误认为播放列表 | 暂时关闭「自动连播」，仅使用手动播放 |
| 时间更新事件丢失 | 视频使用 Media Source Extensions (MSE) 播放 | 刷新页面重新加载 |

### 4.5 扩展导致页面卡顿

| 可能原因 | 排查思路 | 解决方法 |
|----------|----------|----------|
| 调试模式开启 | 日志输出过多消耗性能 | 关闭「调试模式」 |
| 视频元素过多 | 页面存在大量 `<video>` 元素 | 扩展会自动扫描所有帧，如果页面有几十个视频，可能影响性能 |
| 无限循环检测 | 自动点击逻辑陷入死循环 | 立即关闭「自动连播」开关，刷新页面 |

### 4.6 其他问题

如果以上方法均无法解决，请按以下步骤提供反馈：

1. 开启「调试模式」。
2. 复现问题。
3. 点击面板中的「日志」按钮。
4. 点击「复制日志」。
5. 将日志内容连同问题描述一起提交至项目的 GitHub Issues 页面。

---

## 五、进阶阅读

| 资源 | 说明 | 链接 |
|------|------|------|
| README.md | 项目概述、功能列表、安装方式 | 项目根目录 `README.md` |
| CHANGELOG.md | 版本更新记录与历史变更 | 项目根目录 `CHANGELOG.md` |
| manifest.json | 扩展权限声明与配置说明 | 项目根目录 `manifest.json` |
| content.js | 核心逻辑源码（含详细注释） | 项目根目录 `content.js` |
| content.css | 悬浮面板样式源码 | 项目根目录 `content.css` |

**推荐阅读路径**：

1. 先读 `README.md` 了解项目全貌。
2. 阅读 `content.js` 顶部的配置对象（`DEFAULTS`），了解所有可调参数。
3. 搜索 `content.js` 中的 `TODO` 或 `FIXME` 注释，了解已知限制与待优化点。
4. 查看 `CHANGELOG.md` 了解版本演进思路。

---

*文档结束。如果本文未能覆盖你的问题，请通过项目 Issues 渠道反馈。*