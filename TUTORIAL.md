# 自动连播助手 使用教程

## 前置准备

在开始使用前，请确认你的环境满足以下条件：

| 项目 | 要求 |
|------|------|
| 浏览器 | Chrome 88+ 或 Edge 88+（基于 Chromium 内核） |
| 操作系统 | Windows / macOS / Linux 均可 |
| 项目文件 | 已下载并解压「自动连播助手」源码包 |

> 本项目为浏览器扩展，**无需安装 Node.js 或任何依赖**，源码即开即用。

---

## 5 分钟跑起来（最小可用示例）

### 第 1 步：打开扩展管理页面

- **Chrome**：在地址栏输入 `chrome://extensions` 并回车
- **Edge**：在地址栏输入 `edge://extensions` 并回车

### 第 2 步：开启开发者模式

找到页面右上角（或左侧）的 **「开发者模式」** 开关，点击将其打开。

### 第 3 步：加载扩展

1. 点击页面左上角的 **「加载已解压的扩展程序」** 按钮
2. 在弹出的文件选择窗口中，**选中本项目根目录**（即包含 `manifest.json` 的文件夹）
3. 点击「选择文件夹」确认

加载成功后，扩展列表中会出现「自动连播助手」的卡片。

### 第 4 步：验证生效

1. 打开任意视频网站（如 Bilibili、YouTube、中国大学 MOOC 等）
2. 等待页面加载完成
3. 观察页面**右下角**——出现深色悬浮面板即表示扩展已生效

> **预期效果**：面板显示视频标题、当前进度百分比，并带有「连播」开关。

---

## 进阶用法

### 一、视频暂停自动续播

**功能说明**：当视频因网络缓冲、手动暂停或切后台等原因停止播放时，扩展会自动点击播放按钮恢复播放。

**操作步骤**：

1. 确保悬浮面板上的状态指示为绿色 **「运行中」**
2. 正常播放任意视频
3. 手动点击暂停，观察 3~5 秒——视频会自动恢复播放

> **注意**：若你希望暂停后不自动续播（例如需要记笔记），请点击面板上的「连播」开关，将其切换为灰色 **「已暂停」** 状态。

### 二、自动识别播放列表并连播下一集

**功能说明**：扩展自动识别页面中的课程目录/播放列表，当前视频播放完毕后自动点击下一集。

**操作步骤**：

1. 打开一个包含多集视频的课程页面（如 MOOC 课程、Bilibili 合集）
2. 确认悬浮面板中「列表识别」区域显示了课程章节列表
3. 正常播放第一集，播放至结束后观察——扩展会自动点击下一集

**面板显示说明**：

- 视频进度条：当前视频播放进度
- 课程总进度：整个列表已完成百分比（含漏看课时自动补齐）

### 三、穿透 iframe 定位课程目录

**适用场景**：部分网课平台（如智慧树、学习通）将视频嵌入 iframe 中，且目录在顶层页面。

**功能说明**：扩展自动穿透 iframe 边界，在顶层页面定位并操作课程目录。

**操作步骤**：

1. 打开此类嵌套结构的课程页面
2. 无需任何手动配置，扩展自动检测 iframe 层级
3. 观察面板——若页面存在嵌套视频，面板会显示「已连接内嵌播放器」提示

> **提示**：若遇到 iframe 跨域限制导致无法操作，请参考「排错指南」第 4 条。

### 四、漏看课时自动补齐

**功能说明**：当播放列表中存在未观看（进度为 0%）的课时时，扩展会在当前列表播完后自动跳转至第一个漏看的课时。

**操作步骤**：

1. 在课程目录中，手动跳过某一集不观看
2. 从其他集数开始播放，直至列表末尾
3. 扩展会自动跳转至漏看的那一集开始播放

### 五、悬浮面板操作

面板支持以下交互操作：

| 操作 | 方法 |
|------|------|
| **拖拽移动** | 按住面板顶部标题栏，拖动至任意位置 |
| **折叠/展开** | 点击面板右上角的 `—` 图标折叠为小圆球；点击圆球恢复 |
| **查看调试日志** | 点击面板底部的「日志」按钮，展开实时日志区域 |
| **开关连播** | 点击面板顶部的「连播」开关按钮 |

---

## 排错指南

### 1. 扩展已加载，但打开视频页面没有悬浮面板

**可能原因**：当前网站不在支持列表中。

**排查步骤**：

1. 确认访问的域名是否在以下列表中：
   ```
   webtrn.cn / bilibili.com / youku.com / iqiyi.com / v.qq.com /
   study.163.com / icourse163.org / xuetangx.com / zhihuishu.com /
   imooc.com / chaoxing.com / open.163.com / ke.qq.com / gaodun.com /
   gaoxiaobang.com / duobei.com / xuexi.cn / docin.com / doc88.com /
   youtube.com / vimeo.com / twitch.tv / douyin.com / kuaishou.com /
   huya.com / douyu.com
   ```
2. 若不在列表中：该页面默认不启用扩展（避免干扰无关网站）
3. 若在列表中但仍无面板：打开开发者工具（F12）→ Console 标签，查看是否有红色报错信息

### 2. 视频暂停后没有自动续播

**排查步骤**：

1. 检查面板状态是否为绿色「运行中」——若为灰色，点击开关重新启用
2. 确认视频是否因**手动暂停**触发——扩展对用户手动暂停的响应有 3 秒延迟，请稍候
3. 打开面板「日志」，查看是否有 `[pause] detected, resuming...` 记录
4. 若日志无记录，刷新页面后重试

### 3. 播放列表识别失败（面板不显示课程列表）

**排查步骤**：

1. 确认当前页面确实包含**多集列表**（单集视频无列表可识别）
2. 打开「日志」，查看是否有 `[list] scanning...` 输出
3. 若列表是动态加载（滚动加载更多），请先滚动页面加载完整列表
4. 若列表位于 iframe 内，确认扩展提示「已连接内嵌播放器」

### 4. iframe 内视频无法控制（跨域限制）

**现象**：面板显示已连接，但进度不更新、无法自动切集。

**原因**：浏览器安全策略限制跨域 iframe 操作。

**解决方案**：

1. 确认浏览器是否允许扩展访问站点数据（扩展详情 → 网站权限 → 允许）
2. 部分平台（如学习通）需在**新标签页**打开课程，而非嵌套在门户内
3. 若仍无法解决，请到项目 GitHub Issues 提交反馈，附带控制台报错截图

### 5. 扩展更新后功能异常

**操作步骤**：

1. 打开 `chrome://extensions` 或 `edge://extensions`
2. 找到「自动连播助手」，点击**刷新按钮**（圆形箭头图标）
3. 刷新已打开的视频页面，重新加载

---

## 进阶阅读

| 资源 | 说明 |
|------|------|
| **项目 README** | 项目根目录下的 `README.md`，包含完整功能列表与架构说明 |
| **CHANGELOG.md** | 版本更新记录，查看各版本新增功能与修复内容 |
| **源码导读** | 核心逻辑位于 `content.js`，样式定义在 `content.css`，可自行修改定制 |
| **Manifest 配置** | 查看 `manifest.json` 了解扩展权限声明与注入规则 |

---

> **提示**：本项目为 MIT 开源协议，欢迎 Fork 二次开发。若使用中遇到问题，请携带浏览器版本、网站地址及调试日志提交 Issue。