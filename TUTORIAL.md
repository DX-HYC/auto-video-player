# 自动连播助手 使用教程

---

## 一、前置准备

在开始之前，请确认你的环境满足以下条件：

| 项目 | 要求 |
|------|------|
| 浏览器 | Chrome 88+ 或 Edge 88+（均基于 Chromium） |
| 操作系统 | Windows / macOS / Linux 均可 |
| 项目文件 | 已下载并解压「自动连播助手」源码包，或已克隆 Git 仓库 |

> **提示**：本项目是浏览器扩展，不需要安装 Node.js 或任何构建工具，源码即开即用。

---

## 二、5 分钟跑起来（最小可用示例）

### 第 1 步：打开扩展管理页面

- **Chrome**：在地址栏输入 `chrome://extensions` 并回车
- **Edge**：在地址栏输入 `edge://extensions` 并回车

### 第 2 步：开启「开发者模式」

在扩展管理页面的**右上角**（Chrome）或**左侧面板底部**（Edge），找到「开发者模式」开关并打开。

### 第 3 步：加载扩展

1. 点击页面左上角的 **「加载已解压的扩展程序」** 按钮。
2. 在弹出的文件选择窗口中，**选中本项目根目录**（即包含 `manifest.json` 文件的文件夹）。
3. 点击「选择文件夹」确认。

加载成功后，扩展列表中会出现「自动连播助手」的卡片。

### 第 4 步：验证生效

1. 打开任意包含视频的网页（如 Bilibili、YouTube、腾讯课堂等）。
2. 等待页面加载完成，**右下角**会出现一个深色悬浮面板，标题为「自动连播助手」。
3. 面板上显示绿色「运行中」状态标签，即表示扩展已生效。

> ✅ **完成！** 此时播放任意视频，播完后扩展会自动尝试播放下一集。

---

## 三、进阶用法

### 3.1 控制面板操作

悬浮面板是扩展的主要交互入口，支持以下操作：

| 操作 | 方法 |
|------|------|
| **拖拽移动** | 按住面板顶部标题栏（显示「自动连播助手」的区域），拖动到任意位置 |
| **折叠/展开** | 点击面板右上角的 `—`（最小化）按钮，面板会收起为一个小圆标；再次点击圆标即可展开 |
| **查看实时状态** | 面板主体区域显示当前视频进度百分比和课程总进度百分比 |

### 3.2 功能开关配置

点击面板底部的 **「设置」** 按钮（齿轮图标），展开配置项：

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| **启用扩展** | 开启 | 总开关，关闭后所有自动行为停止 |
| **自动续播** | 关闭 | 视频因网络卡顿或手动暂停后，自动恢复播放。**注意**：默认关闭以降低被平台风控的风险，建议仅在需要时开启 |
| **自动连播** | 开启 | 当前视频播放结束后，自动切换到下一集 |
| **续播延迟** | 1200ms | 自动续播前的等待时间，可适当调大避免被平台检测 |
| **调试日志** | 关闭 | 开启后在面板中显示详细运行日志，用于排查问题 |

> **配置保存**：所有修改即时生效，并自动保存到浏览器存储中，刷新页面后不会丢失。

### 3.3 自动识别与穿透 iframe

扩展会自动扫描页面中所有 `<video>` 元素，包括嵌套在 **iframe** 和 **Shadow DOM** 中的视频。

- **无需任何手动配置**，打开视频页面即自动识别。
- 对于使用 iframe 嵌入播放器的网站（如多数课程平台），扩展会自动「穿透」iframe 定位到实际的视频元素和课程目录。

### 3.4 播放列表与自动连播

扩展会自动识别页面中的播放列表/课程目录，并建立播放顺序：

1. **自动定位目录**：扩展会扫描页面中的课程列表（含 iframe 内部），提取所有课时链接。
2. **播完自动切换**：当前视频播放结束后，自动点击下一个课时。
3. **漏看自动补齐**：如果检测到目录中有未观看的课时（跳过未播），扩展会优先补播漏看的课时，再继续后续内容。

> **示例**：某课程目录共 10 集，你从第 5 集开始看，播完第 5 集后扩展会先跳回第 1 集补齐，而不是直接播第 6 集。

### 3.5 查看调试日志

当遇到异常行为时，建议开启调试日志：

1. 点击面板底部的 **「设置」**。
2. 打开 **「调试日志」** 开关。
3. 面板主体区域会滚动显示运行日志，包括：
   - 扫描到的视频元素数量
   - iframe 穿透结果
   - 播放状态切换事件
   - 自动切换动作记录

> **提示**：日志仅保存在当前页面内存中，刷新页面即清空。如需反馈问题，请截图日志内容。

---

## 四、排错指南

### 现象 1：打开视频页面后，右下角没有出现悬浮面板

| 可能原因 | 排查与解决 |
|----------|------------|
| 扩展未正确加载 | 回到 `chrome://extensions`，确认扩展卡片存在且开关为开启状态 |
| 页面为浏览器内置页面 | Chrome 网上应用店、`chrome://` 开头的页面禁止注入扩展脚本，请换普通网站测试 |
| 扩展冲突 | 尝试在无痕模式（需在扩展详情中允许无痕模式）或禁用其他视频类扩展后重试 |
| 页面未触发加载 | 刷新页面，等待 2~3 秒再观察 |

### 现象 2：视频播完后没有自动连播下一集

| 可能原因 | 排查与解决 |
|----------|------------|
| 页面没有可识别的播放列表 | 打开调试日志，确认是否输出「未找到播放列表」相关记录。部分网站需手动进入课程目录页 |
| 自动连播开关被关闭 | 打开面板设置，确认「自动连播」为开启状态 |
| 视频在 iframe 中且未穿透成功 | 查看日志中 iframe 扫描记录。若失败，请反馈项目作者并提供网站地址 |
| 播放列表结构特殊 | 部分自定义播放器无法自动识别，需等待后续版本适配 |

### 现象 3：视频暂停后没有自动续播

| 可能原因 | 排查与解决 |
|----------|------------|
| 「自动续播」默认关闭 | 这是**有意设计**，为降低被平台检测的风险。请到设置中手动开启 |
| 续播延迟过短 | 尝试将「续播延迟」调大至 2000ms 以上 |
| 平台主动拦截 | 部分网站会检测自动播放行为，若开启后仍无效，建议保持关闭 |

### 现象 4：面板显示进度与实际不符

| 可能原因 | 排查与解决 |
|----------|------------|
| 页面有多个视频元素 | 扩展默认跟踪**当前正在播放**的视频。若页面有广告视频等干扰元素，请关闭广告或等待其结束 |
| 进度未及时刷新 | 面板进度每 1 秒自动刷新一次，若长时间不变，尝试点击视频画面触发播放事件 |

### 现象 5：扩展导致页面卡顿

| 可能原因 | 排查与解决 |
|----------|------------|
| 页面包含大量 iframe | 扩展会递归扫描所有 iframe，极端情况下可能影响性能。可暂时关闭扩展，或反馈作者优化 |
| 调试日志长时间开启 | 关闭「调试日志」开关，减少内存占用 |

---

## 五、进阶阅读

- **项目完整文档**：参见仓库根目录的 `README.md`
- **更新记录**：参见 `CHANGELOG.md`，了解各版本功能变化
- **源码结构**：
  - `manifest.json` — 扩展清单文件（Manifest V3）
  - `content.js` — 核心逻辑（视频识别、自动连播、iframe 穿透）
  - `content.css` — 悬浮面板样式
  - `popup.html/js` — 浏览器工具栏弹出界面（如有）
- **问题反馈**：如果遇到无法解决的问题，请在 GitHub Issues 中提交，附上：
  1. 浏览器版本
  2. 问题网站 URL
  3. 开启调试日志后的截图

---

> **最后提示**：本扩展仅用于提升个人学习效率，请合理使用，遵守各视频平台的用户协议。