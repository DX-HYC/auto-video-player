# 自动连播助手 使用教程

---

## 一、前置准备

### 1.1 环境要求

| 项目 | 要求 |
|------|------|
| 浏览器 | Chrome 88+ 或 Edge 88+（均基于 Chromium） |
| 操作系统 | Windows / macOS / Linux 均可 |
| 技术基础 | 无需编程经验，但需能操作浏览器扩展管理页 |

### 1.2 获取源码

```bash
# 方式一：Git 克隆（推荐）
git clone https://github.com/your-repo/auto-video-player.git
cd auto-video-player

# 方式二：直接下载 ZIP
# 访问项目主页 → Code → Download ZIP → 解压到本地任意目录
```

> **注意**：解压后请确认目录内包含 `manifest.json` 文件，这是扩展的入口配置。

---

## 二、5 分钟跑起来（最小可用示例）

### 2.1 加载扩展（Chrome 示例）

1. 打开 Chrome，在地址栏输入 `chrome://extensions` 并回车
2. 打开页面右上角的 **「开发者模式」** 开关
3. 点击左上角 **「加载已解压的扩展程序」** 按钮
4. 在弹出的文件选择器中，**选中项目根目录**（包含 `manifest.json` 的文件夹）
5. 确认后，扩展列表中出现「自动连播助手」卡片，即加载成功

> **Edge 用户**：地址栏输入 `edge://extensions`，其余步骤完全一致。

### 2.2 验证是否生效

1. 打开任意视频网站（如 B 站、YouTube、慕课网等）
2. 等待 1-2 秒，观察**页面右下角**是否出现悬浮面板
3. 面板标题显示「自动连播助手」，右侧状态灯为绿色「运行中」

### 2.3 首次使用建议

| 操作 | 说明 |
|------|------|
| 播放任意视频 | 面板中「当前进度」开始实时更新 |
| 手动暂停视频 | 观察是否在 1.2 秒后自动恢复播放（默认关闭，需手动开启） |
| 播放至视频结束 | 观察是否自动跳转至下一集 |

> **提示**：若面板未出现，请先参考「排错指南」第 4.1 节。

---

## 三、进阶用法

### 3.1 功能总览

悬浮面板从上到下依次为：

```
┌─────────────────────────────┐
│ 自动连播助手    ● 运行中     │  ← 标题栏 + 状态灯
│                             │
│ 总进度: 35.2%  ████████░░░  │  ← 课程总进度
│ 当前: 第3课/共12课          │  ← 当前视频信息
│ 当前进度: 45%  ████░░░░░░░  │  ← 单视频进度
│                             │
│ [⏸ 暂停自动连播] [📋 复制目录]│  ← 操作按钮
│ [⚙ 设置] [🧪 调试日志]      │
└─────────────────────────────┘
```

### 3.2 自动续播（暂停恢复）

**功能说明**：检测到视频因网络卡顿、误触等原因暂停时，自动点击播放。

**开启方法**：

1. 点击面板中的 **「⚙ 设置」** 按钮
2. 勾选 **「自动续播」** 选项
3. 可调整 **「恢复延迟」**（默认 1200ms，即暂停后 1.2 秒自动恢复）

> **安全提示**：部分平台会检测自动续播行为，默认关闭以降低风险。建议仅在网络不稳定时开启。

### 3.3 自动连播（播完自动下一集）

**功能说明**：视频播放结束后，自动点击「下一集」按钮，实现连续播放。

**适用场景**：
- 课程目录在 iframe 中（如慕课网、学堂在线）
- 播放列表为动态加载（如 YouTube 播放列表）
- 常规翻页式列表（如 B 站合集）

**开启方法**：

1. 点击 **「⚙ 设置」**
2. 确认 **「自动连播」** 已勾选（默认开启）
3. 播放任意视频至结束，观察是否自动跳转

**漏看补齐功能**：若检测到课程目录中有未观看的课时（进度 < 95%），会自动跳转至最早未完成的一集。

### 3.4 实时进度显示

面板实时显示两类进度：

| 进度类型 | 含义 | 示例 |
|----------|------|------|
| 当前进度 | 正在播放视频的播放百分比 | `45%` |
| 总进度 | 整个课程已完成百分比 | `35.2%` |

**总进度计算规则**：
- 自动扫描页面中的播放列表/课程目录
- 统计所有视频条目的完成状态
- 若视频播放超过 95%，视为「已完成」

### 3.5 悬浮面板操作

| 操作 | 方法 |
|------|------|
| 拖拽移动 | 按住面板标题栏（显示「自动连播助手」的区域）拖动 |
| 折叠/展开 | 双击标题栏，面板收起为最小化状态 |
| 查看调试日志 | 点击 **「🧪 调试日志」**，展开日志窗口 |
| 复制课程目录 | 点击 **「📋 复制目录」**，将识别到的目录结构复制到剪贴板 |

### 3.6 调试日志

点击 **「🧪 调试日志」** 后，面板下方展开日志区域，实时显示：

```
[12:01:33] 扫描到 3 个 <video> 元素
[12:01:35] 当前视频: 第3课-网络协议基础.mp4
[12:01:40] 视频暂停，1.2s 后自动恢复
[12:02:10] 视频播放结束，准备跳转下一集
[12:02:11] 已点击「下一集」按钮，跳转至: 第4课
```

> **提示**：日志保留最近 80 条，可通过设置中的「调试模式」开关控制是否输出。

---

## 四、排错指南

### 4.1 面板未出现

| 可能原因 | 排查方法 | 解决方案 |
|----------|----------|----------|
| 扩展未正确加载 | 打开 `chrome://extensions`，确认卡片存在且无红色报错 | 移除后重新「加载已解压的扩展程序」 |
| 页面不支持 | 确认页面包含 `<video>` 标签 | 尝试其他视频网站验证 |
| 扩展被浏览器禁用 | 查看扩展卡片是否有「已停用」标记 | 点击「启用」按钮 |
| 浏览器缓存异常 | 刷新目标页面（Ctrl+Shift+R 强制刷新） | 若无效，重启浏览器 |

### 4.2 自动连播不生效

| 可能原因 | 排查方法 | 解决方案 |
|----------|----------|----------|
| 设置未开启 | 点击「⚙ 设置」，确认「自动连播」为勾选状态 | 勾选后重新播放视频测试 |
| 未识别到播放列表 | 点击「📋 复制目录」，检查复制内容是否为空 | 若为空，说明页面结构特殊，需手动点击下一集 |
| 按钮选择器失效 | 查看调试日志，确认是否输出「未找到下一集按钮」 | 反馈至项目 Issue，附上日志 |
| iframe 跨域限制 | 确认页面是否有跨域 iframe | 扩展已尝试穿透，若失败需反馈 |

### 4.3 自动续播不生效

| 可能原因 | 排查方法 | 解决方案 |
|----------|----------|----------|
| 功能未开启 | 确认「自动续播」选项已勾选 | 默认关闭，需手动开启 |
| 恢复延迟过短 | 检查「恢复延迟」设置 | 尝试调大至 2000ms |
| 视频为直播流 | 直播视频暂停后无法自动恢复 | 属正常现象，非直播视频不受影响 |

### 4.4 进度显示异常

| 可能原因 | 排查方法 | 解决方案 |
|----------|----------|----------|
| 页面结构复杂 | 查看调试日志，确认扫描到的视频数量 | 反馈至 Issue，附上页面 URL |
| 多视频同屏 | 页面同时存在多个 `<video>` | 扩展默认追踪「当前正在播放」的视频 |
| 进度不刷新 | 确认视频时间更新事件是否触发 | 尝试切换清晰度或刷新页面 |

### 4.5 扩展与其他插件冲突

若同时使用其他视频增强插件（如视频加速、去广告等），可能导致冲突。建议：

1. 暂时禁用其他视频相关扩展
2. 逐一启用，定位冲突源
3. 在项目 Issue 中反馈冲突详情

---

## 五、进阶阅读

| 资源 | 说明 | 链接 |
|------|------|------|
| README | 项目简介、功能列表、截图 | 项目主页 → README.md |
| Wiki | 架构说明、开发指南、常见问题 | 项目主页 → Wiki 标签 |
| Issue | 报告 Bug、功能建议、讨论 | 项目主页 → Issues |
| 源码结构 | 核心逻辑在 `content.js`，样式在 `content.css` | 项目根目录 |
| 图标生成 | 运行 `python gen_icons.py` 可重新生成扩展图标 | 需 Python 3 环境 |

**推荐阅读路径**：
1. 先阅读 `README.md` 了解项目全貌
2. 遇到问题优先搜索 Wiki 的「常见问题」章节
3. 仍无法解决，在 Issue 中搜索关键词，或新建 Issue 并附上调试日志

---

> **提示**：本项目遵循 MIT 协议，欢迎 Fork 二次开发。若觉得好用，别忘了给个 Star ⭐ 支持一下！