# 自动连播助手使用教程

## 前置准备

### 环境要求

| 项目 | 要求 |
|------|------|
| 浏览器 | Chrome 88+ 或 Edge 88+（基于 Chromium 内核） |
| 操作系统 | Windows / macOS / Linux 均可 |
| 技术基础 | 无需编程经验，但需能解压 ZIP 文件 |

### 获取源码

从项目仓库下载源码并解压到本地目录，例如 `D:\projects\auto-video-player` 或 `~/Projects/auto-video-player`。

> **注意**：目录路径中请勿包含中文或空格，否则可能导致扩展加载失败。

---

## 5 分钟跑起来（最小可用示例）

### 第 1 步：打开扩展管理页面

- **Chrome**：地址栏输入 `chrome://extensions` 并回车
- **Edge**：地址栏输入 `edge://extensions` 并回车

### 第 2 步：开启开发者模式

在扩展管理页面**右上角**找到「开发者模式」开关，点击将其打开。开启后，页面左上角会出现「加载已解压的扩展程序」按钮。

### 第 3 步：加载扩展

1. 点击「加载已解压的扩展程序」按钮
2. 在弹出的文件选择器中，**选中项目根目录**（即包含 `manifest.json` 文件的文件夹）
3. 点击「选择文件夹」确认

加载成功后，扩展列表中会出现「自动连播助手」的卡片。

### 第 4 步：验证生效

1. 打开任意支持的视频网站（如 Bilibili、YouTube、中国大学 MOOC 等）
2. 等待页面加载完成
3. 观察页面**右下角**——出现半透明悬浮面板即表示扩展已生效

> **验证点**：面板顶部显示绿色「运行中」状态标识，下方显示当前视频的实时进度。

---

## 进阶用法

### 一、自动续播（视频暂停自动恢复）

**功能说明**：当视频因网络缓冲、手动暂停或切后台等原因停止播放时，扩展会自动点击播放按钮恢复播放。

**使用场景**：
- 网课学习时切到其他窗口，视频自动暂停后无需手动回来点播放
- 网络波动导致视频卡顿暂停，扩展会自动恢复

**操作方式**：无需任何设置，扩展默认启用此功能。

> **提示**：若某些网站播放器结构特殊导致续播失败，请参考「排错指南」中的调试日志功能。

---

### 二、自动连播（播完自动下一集）

**功能说明**：当前视频播放结束后，扩展会自动识别页面中的播放列表（课程目录），自动点击下一集进行播放。

**使用场景**：
- 刷剧时无需手动点下一集
- 网课学习时自动连续播放多个课时

**操作方式**：无需任何设置，扩展默认启用此功能。

**特殊能力**：
- **穿透 iframe**：即使课程目录嵌在多层 iframe 中，扩展也能正确定位
- **漏看补齐**：如果检测到目录中有未看过的课时（进度低于 95%），会自动优先播放漏看的课时，而不是按顺序播放下一集

> **示例**：某课程共 10 集，你看到第 5 集后直接跳到第 8 集观看。第 8 集播完后，扩展会先自动播放第 6 集（漏看的），而不是第 9 集。

---

### 三、实时进度显示

**功能说明**：悬浮面板实时显示两类进度信息：

| 进度类型 | 显示内容 | 示例 |
|---------|---------|------|
| 当前视频进度 | 正在播放的视频的播放百分比 | `当前：45%` |
| 课程总进度 | 整个播放列表的平均完成度 | `总进度：32%（3/10 集）` |

**使用场景**：
- 快速了解当前视频看到哪了
- 评估整个课程/剧集的学习完成情况

**操作方式**：无需设置，打开面板即可看到。

---

### 四、悬浮面板操作

#### 拖拽移动

- 鼠标左键按住面板**顶部标题栏**（显示「自动连播助手」的区域），拖动到屏幕任意位置后松开

#### 折叠 / 展开

- 点击面板右上角的 **`—`**（折叠）按钮，面板会收起为一个小圆球
- 点击小圆球即可重新展开面板

#### 查看调试日志

1. 点击面板底部的「调试日志」区域（或「展开日志」按钮）
2. 日志面板会向上展开，实时滚动显示扩展的运行状态信息

**日志内容示例**：
```
[12:01:33] 检测到视频元素，开始监听
[12:01:35] 视频暂停，尝试自动续播...
[12:01:35] 续播成功
[12:05:10] 当前视频播放结束，查找下一集...
[12:05:11] 找到下一集，开始播放
```

> **用途**：当扩展行为异常时，日志是排查问题的第一手资料。

---

### 五、支持的网站范围

扩展会在以下类型的网站自动激活：

| 类别 | 示例网站 |
|------|---------|
| 视频平台 | Bilibili、YouTube、优酷、爱奇艺、腾讯视频、抖音、快手、虎牙、斗鱼 |
| 网课平台 | 中国大学 MOOC、学堂在线、智慧树、网易云课堂、慕课网、超星学习通 |
| 知识平台 | 学习强国、腾讯课堂、高顿、高小帮、多贝 |
| 文档平台 | 道客巴巴、豆丁网（含视频内容时） |

> **注意**：如果打开其他视频网站（不在上述列表），扩展不会激活，右下角不会出现悬浮面板。

---

## 排错指南

### 现象 1：打开视频网站，右下角没有悬浮面板

| 可能原因 | 排查方法 | 解决方案 |
|---------|---------|---------|
| 扩展未正确加载 | 打开 `chrome://extensions`，确认「自动连播助手」卡片存在且无红色报错 | 移除扩展后重新「加载已解压的扩展程序」 |
| 网站不在支持列表 | 确认当前网站域名是否在「支持的网站范围」列表中 | 使用支持的网站，或联系开发者添加 |
| 页面在 iframe 中 | 部分网站视频嵌在 iframe 中，扩展只在顶层页面显示面板 | 确认地址栏 URL 是否为最终视频页面 |
| 扩展未刷新 | 修改代码后未重新加载扩展 | 在扩展管理页面点击扩展卡片上的「刷新」按钮 |

### 现象 2：视频暂停后没有自动续播

| 可能原因 | 排查方法 | 解决方案 |
|---------|---------|---------|
| 播放器结构特殊 | 打开面板「调试日志」，查看是否有「尝试自动续播」记录 | 若日志显示已尝试但失败，可能是播放器自定义了暂停逻辑，请反馈给开发者 |
| 视频被手动暂停 | 扩展默认只处理非用户主动暂停的情况 | 手动暂停后扩展不会立即续播，等待数秒观察是否恢复 |
| 多个视频元素 | 页面存在多个 `<video>` 标签，扩展可能监听错对象 | 查看日志确认监听的视频是否为当前正在播放的视频 |

### 现象 3：播完没有自动连播下一集

| 可能原因 | 排查方法 | 解决方案 |
|---------|---------|---------|
| 未识别到播放列表 | 查看日志是否有「查找下一集」记录 | 若日志显示未找到播放列表，说明该网站页面结构不支持，请反馈给开发者 |
| 播放列表在 iframe 中 | 扩展已支持穿透 iframe，但个别网站层级过深 | 手动点击一次下一集，观察扩展是否能学习并继续 |
| 漏看补齐逻辑干扰 | 扩展检测到有漏看的课时，优先播放漏看内容 | 这是正常行为，如需按顺序播放，请将漏看课时看完 |

### 现象 4：面板显示异常或遮挡内容

| 可能原因 | 排查方法 | 解决方案 |
|---------|---------|---------|
| 面板位置不合适 | 面板默认在右下角 | 按住面板标题栏拖拽到合适位置 |
| 面板遮挡视频字幕 | 面板尺寸过大 | 点击「折叠」按钮收起面板 |
| 样式冲突 | 个别网站 CSS 影响面板显示 | 刷新页面重新加载扩展 |

### 现象 5：扩展加载报错

| 错误提示 | 解决方案 |
|---------|---------|
| `Failed to load extension` | 检查选择的目录是否正确（必须包含 `manifest.json`） |
| `Manifest file is missing or unreadable` | 确认目录路径无中文/空格，重新解压源码 |
| `Permission denied` | 检查目录是否被系统保护，更换目录后重试 |

---

## 进阶阅读

- **项目 README**：项目根目录下的 `README.md` 包含完整的项目介绍、功能清单和开发计划
- **更新日志**：`CHANGELOG.md` 记录每个版本的变更内容，了解最新功能与修复
- **源码结构**：
  - `manifest.json` — 扩展配置文件（权限声明、脚本注册）
  - `content.js` — 核心逻辑（视频监听、自动连播、进度计算）
  - `content.css` — 悬浮面板样式
  - `popup/` — 扩展弹窗页面（如有）
- **二次开发**：如需修改功能，编辑 `content.js` 后，在扩展管理页面点击「刷新」按钮即可生效

---

> **遇到问题？** 先查看「调试日志」获取运行信息，再对照「排错指南」排查。若仍无法解决，请携带日志内容向项目维护者反馈。