# 自动连播助手 · 使用教程

> 自动识别网页视频与播放列表：视频暂停自动续播，播放结束自动连播（Chrome / Edge 扩展）

---

## 一、前置准备

在开始之前，请确认你的环境满足以下条件：

| 项目 | 要求 |
|------|------|
| 浏览器 | Chrome 88+ 或 Edge 88+（基于 Chromium 内核） |
| 操作系统 | Windows / macOS / Linux 均可 |
| 项目文件 | 已下载并解压 `自动连播助手` 源码包（或已克隆 Git 仓库） |
| 网络 | 无需特殊网络，扩展完全本地运行 |

**获取源码**（任选其一）：

```bash
# 方式一：Git 克隆（推荐）
git clone https://github.com/your-repo/auto-video-player.git

# 方式二：直接下载 ZIP
# 访问仓库页面 → Code → Download ZIP → 解压到本地目录
```

> ⚠️ **注意**：请确保解压后的目录结构完整，根目录下应包含 `manifest.json`、`content.js`、`content.css` 等文件。

---

## 二、5 分钟跑起来（最小可用示例）

### 2.1 加载扩展

1. 打开 Chrome 浏览器，在地址栏输入 `chrome://extensions` 并回车（Edge 用户输入 `edge://extensions`）。
2. 在页面**右上角**找到「开发者模式」开关，点击**开启**。
3. 开启后，页面左上角会出现「加载已解压的扩展程序」按钮，点击它。
4. 在弹出的文件选择窗口中，**选中项目根目录**（即包含 `manifest.json` 的文件夹），点击「选择文件夹」。

**验证安装成功**：

- 扩展列表中应出现「自动连播助手」卡片，且开关处于**开启**状态（蓝色）。
- 如果卡片显示红色错误提示，请检查目录是否选对，或参考文末「排错指南」。

### 2.2 首次使用

1. 打开任意支持的视频网站（如 Bilibili、YouTube、中国大学 MOOC 等）。
2. 播放任意一个视频。
3. 观察页面**右下角**——出现半透明悬浮面板，标题为「自动连播助手」，状态显示绿色「运行中」。
4. 此时扩展已生效，你可以尝试：

   - **暂停视频** → 等待 3 秒，视频将自动恢复播放。
   - **让视频播完** → 扩展将自动识别播放列表并播放下一个视频。

> 💡 **提示**：如果页面右下角没有出现面板，请点击浏览器工具栏中的拼图图标（扩展程序菜单），找到「自动连播助手」，点击图钉图标将其固定，然后刷新视频页面。

### 2.3 卸载 / 停用

- **临时停用**：在 `chrome://extensions` 页面，关闭「自动连播助手」的开关即可。
- **彻底移除**：点击「自动连播助手」卡片上的「移除」按钮。

---

## 三、进阶用法

### 3.1 悬浮面板操作

面板是扩展的控制中心，支持以下操作：

| 操作 | 方法 |
|------|------|
| **拖拽移动** | 按住面板顶部标题栏（「自动连播助手」文字区域），拖动到屏幕任意位置 |
| **折叠 / 展开** | 点击面板右上角的「—」按钮折叠为最小化状态；再次点击「+」按钮展开 |
| **查看运行状态** | 面板顶部右侧显示绿色「运行中」或灰色「已暂停」徽标 |
| **实时进度** | 面板主体区域显示当前视频播放进度条，以及课程总进度百分比（如 `第 3/12 集 · 总进度 25%`） |

### 3.2 自动续播设置

扩展默认在视频暂停 3 秒后自动恢复播放。如需调整：

1. 点击面板底部的「设置」按钮（齿轮图标），展开设置区域。
2. 找到「暂停续播延时」选项，可设置为：
   - **立即恢复**（0 秒）
   - **3 秒**（默认）
   - **5 秒**
   - **10 秒**
3. 设置会自动保存，对所有网站生效。

> ⚠️ **注意**：如果你手动暂停视频并希望在较长时间内保持暂停（例如记笔记），建议将延时调至 10 秒，或直接点击面板上的「暂停自动续播」开关。

### 3.3 播放列表与自动连播

扩展会自动识别页面中的播放列表（如课程章节列表、视频合集），并支持以下行为：

- **自动连播**：当前视频播放结束后，自动点击列表中的下一集。
- **漏看补齐**：如果检测到列表中某集从未播放过（进度为 0），扩展会优先播放该集，确保不漏看。
- **循环模式**（可选）：

  1. 在面板设置中，找到「播放模式」下拉菜单。
  2. 支持三种模式：
     - **顺序播放**（默认）：播完最后一集后停止。
     - **列表循环**：播完最后一集后，自动回到第一集继续播放。
     - **单集循环**：当前视频播放结束后重新播放当前集。

### 3.4 跨 iframe 视频识别

部分网课平台（如智慧树、中国大学 MOOC）将视频嵌入在 iframe 中。扩展已内置穿透机制：

- **自动检测**：扩展会自动扫描页面内所有 iframe，并尝试访问其中的 `<video>` 元素。
- **无需额外配置**：只要 iframe 内视频可播放，扩展即可控制。

> ⚠️ **注意**：受浏览器同源策略限制，部分跨域 iframe 可能无法被检测。此时请尝试在 iframe 页面内直接使用扩展（即点击 iframe 区域，让 iframe 获得焦点）。

### 3.5 调试日志

当遇到问题时，可以查看调试日志定位原因：

1. 在面板底部，点击「日志」按钮（终端图标），展开日志区域。
2. 日志按时间顺序显示扩展的关键操作，例如：
   - `[检测] 发现 2 个 <video> 元素`
   - `[播放] 当前视频暂停，3 秒后自动续播`
   - `[连播] 第 3 集播放结束，准备播放第 4 集`
   - `[错误] 未找到下一个视频链接`
3. 点击「清空」按钮可清除日志。

---

## 四、排错指南

### 4.1 扩展无法加载

| 现象 | 可能原因 | 解决方法 |
|------|----------|----------|
| 加载时提示「清单文件缺失或不可读」 | 选错了目录 | 确认选中的目录下直接包含 `manifest.json`，不要多选一层文件夹 |
| 加载后显示「此扩展程序可能已损坏」 | 文件不完整 | 重新下载 ZIP 并解压，或重新 `git pull` |
| 扩展加载成功但图标为灰色 | 扩展未启用 | 在扩展管理页确认开关为开启状态 |

### 4.2 面板不出现

| 现象 | 可能原因 | 解决方法 |
|------|----------|----------|
| 打开视频网站后右下角无面板 | 网站不在支持列表 | 确认域名在 `KNOWN_VIDEO_HOSTS` 列表中（见源码 content.js），或手动将页面加入白名单（见 4.5） |
| 面板出现后又消失 | 页面使用了 SPA 路由跳转 | 刷新页面，或等待几秒让扩展重新注入 |
| 只在部分页面出现 | 扩展仅注入到匹配的域名 | 打开 `chrome://extensions` → 找到本扩展 → 点击「详细信息」→ 查看「网站访问权限」是否包含所需站点 |

### 4.3 自动续播不生效

| 现象 | 可能原因 | 解决方法 |
|------|----------|----------|
| 暂停后视频不恢复 | 延时未到 | 等待超过设定的延时时间（默认 3 秒） |
| 暂停后立即恢复，无法保持暂停 | 延时设置过短 | 在设置中将延时调大，或关闭「自动续播」开关 |
| 点击暂停按钮后仍自动播放 | 页面播放器有自定义逻辑 | 查看调试日志，确认扩展是否识别到暂停事件；若无效，请提交 Issue |

### 4.4 自动连播不工作

| 现象 | 可能原因 | 解决方法 |
|------|----------|----------|
| 视频播完后无动作 | 未识别到播放列表 | 确认页面有可见的列表元素（如课程目录）；部分网站需展开目录 |
| 播完后跳到错误视频 | 列表识别错误 | 查看日志中「连播」相关记录；尝试手动点击列表中的下一集，让扩展学习 |
| 提示「未找到下一个视频」 | 当前是最后一集 | 若需循环播放，在设置中切换为「列表循环」模式 |

### 4.5 手动添加网站白名单

如果某个视频网站不在默认支持列表中，可以手动添加：

1. 打开 `content.js` 文件，找到 `KNOWN_VIDEO_HOSTS` 数组。
2. 添加目标网站的域名，例如：
   ```javascript
   const KNOWN_VIDEO_HOSTS = [
       // ... 现有列表
       'example.com',   // 新增
       'video.example.org'
   ];
   ```
3. 保存文件，回到 `chrome://extensions`，点击「自动连播助手」卡片上的**刷新按钮**（循环箭头图标）。
4. 重新打开目标网站，面板应正常出现。

### 4.6 其他问题

- **问题**：扩展导致页面卡顿。
  - **解决**：在设置中关闭「实时进度显示」，或折叠面板减少渲染开销。
- **问题**：视频播放进度与面板显示不一致。
  - **解决**：点击面板上的「刷新」按钮（刷新图标），重新同步进度。
- **问题**：所有功能均无效，且日志无输出。
  - **解决**：确认扩展已启用且注入成功；尝试在无痕模式下测试（需在扩展详情中启用「允许在无痕模式下运行」）。

---

## 五、进阶阅读

- **项目 README**：包含完整的架构说明、开发环境搭建、贡献指南。  
  → 项目根目录下 `README.md`

- **更新日志**：了解各版本新增功能与修复内容。  
  → 项目根目录下 `CHANGELOG.md`

- **源码结构**：
  ```
  auto-video-player/
  ├── manifest.json      # 扩展清单（MV3）
  ├── content.js         # 核心逻辑：视频识别、自动续播、连播
  ├── content.css        # 悬浮面板样式
  ├── background.js      # 后台脚本（如需）
  ├── icons/             # 扩展图标
  └── README.md          # 项目文档
  ```

- **参与贡献**：欢迎提交 Issue 或 Pull Request，请先阅读 `CONTRIBUTING.md`（如存在）。

- **开源协议**：本项目基于 MIT 协议开源，可自由使用与修改。

---

*文档版本：v1.0 · 最后更新：2026-09-07*