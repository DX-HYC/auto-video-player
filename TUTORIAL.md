## 前置准备

在开始之前，请确认你的环境满足以下条件：

| 项目 | 要求 |
|------|------|
| 浏览器 | Chrome 88+ 或 Edge 88+（基于 Chromium 内核） |
| 操作系统 | Windows / macOS / Linux 均可 |
| 项目文件 | 已下载并解压 `auto-video-player` 项目源码到本地目录 |
| 目标网站 | 支持常见视频/网课平台（B站、优酷、爱奇艺、腾讯视频、中国大学MOOC、智慧树、学习强国等） |

> **提示**：本项目为浏览器扩展，无需安装 Node.js 或任何依赖包，纯前端实现。

---

## 5 分钟跑起来（最小可用示例）

### 第 1 步：打开扩展管理页面

- **Chrome**：地址栏输入 `chrome://extensions` 并回车
- **Edge**：地址栏输入 `edge://extensions` 并回车

### 第 2 步：开启「开发者模式」

在扩展管理页面**右上角**找到「开发者模式」开关，点击将其打开。

### 第 3 步：加载已解压的扩展程序

1. 点击页面左上角的 **「加载已解压的扩展程序」** 按钮
2. 在弹出的文件选择器中，**选中项目根目录**（即包含 `manifest.json` 文件的文件夹）
3. 点击「选择文件夹」确认

加载成功后，扩展列表中会出现「自动连播助手」的卡片。

### 第 4 步：验证生效

1. 打开任意受支持的视频网站（如 `bilibili.com` 或 `study.163.com`）
2. 打开任意一个视频页面
3. 观察页面**右下角** —— 出现悬浮控制面板即表示扩展已生效

**预期效果**：

- 面板显示当前视频的播放进度（如 `12:34 / 45:00`）
- 面板显示课程总进度百分比（如 `课程总进度: 35%`）
- 视频暂停超过 3 秒后自动恢复播放
- 当前集播放结束后自动播放下一集

---

## 进阶用法

### 一、自动续播（视频暂停自动恢复）

**功能说明**：当视频因网络缓冲、手动暂停或其他原因停止播放时，扩展会在短暂延迟后自动点击播放按钮。

**操作步骤**：

1. 确保悬浮面板中的状态标签显示为绿色 **「运行中」**
2. 手动暂停视频，观察 3~5 秒
3. 视频会自动恢复播放

**自定义设置**：

- 点击面板右上角的 **「设置」** 图标（齿轮形状）
- 调整「续播延迟秒数」（默认 3 秒）
- 点击「保存」使设置生效

> **注意**：若视频因用户主动拖动进度条而暂停，扩展会等待更长时间（10 秒）再续播，避免干扰手动操作。

### 二、自动连播（播完自动播放下一集）

**功能说明**：自动识别当前视频所属的播放列表/课程目录，在当前集播放结束后自动点击下一集。

**操作步骤**：

1. 打开一个包含多集视频的页面（如电视剧、课程列表）
2. 确认悬浮面板中「当前集数」显示正确（如 `第 3 集 / 共 20 集`）
3. 正常播放视频，无需任何操作
4. 当前集播完后，扩展会自动点击下一集

**识别逻辑**：

- 扩展会扫描页面 DOM 中的播放列表元素（如 `<ul>` 中包含的链接列表）
- 支持穿透 iframe 定位课程目录（适用于嵌套框架的网课平台）
- 若页面无显式播放列表，扩展会尝试从页面标题或 URL 中推断

### 三、漏看课时自动补齐

**功能说明**：当检测到播放列表中有未观看的课时（进度低于 95%），扩展会在当前集播完后自动跳转到最早未完成的课时。

**适用场景**：

- 网课平台要求所有课时必须看完
- 手动跳过了某些集数

**操作步骤**：

1. 打开课程页面，确保扩展已识别到完整课时列表
2. 直接播放任意一集
3. 当该集播放结束后，扩展会检查列表中是否存在「未完成」的课时
4. 若存在，自动跳转到最早未完成的课时开始播放

**面板提示**：当发生自动补齐跳转时，面板会短暂显示黄色提示 `检测到未完成课时，自动跳转至第 X 集`。

### 四、悬浮面板操作

#### 拖拽移动

- 鼠标左键按住面板**顶部标题栏**（显示「自动连播助手」的区域）
- 拖动到屏幕任意位置后松开

#### 折叠/展开

- 点击面板右上角的 **「—」** 按钮折叠为最小化状态
- 折叠后仅显示一个小圆球图标，点击即可重新展开

#### 查看调试日志

1. 点击面板底部的 **「日志」** 标签页
2. 面板内容切换为日志列表，显示扩展的实时运行记录
3. 日志包含：视频检测、播放状态变化、自动续播动作、连播跳转等事件

**日志级别说明**：

| 级别 | 颜色 | 含义 |
|------|------|------|
| INFO | 白色 | 常规运行信息 |
| SUCCESS | 绿色 | 成功执行的操作 |
| WARN | 黄色 | 非致命警告 |
| ERROR | 红色 | 执行失败或异常 |

### 五、手动控制播放状态

面板提供以下手动控制按钮：

| 按钮 | 功能 |
|------|------|
| ⏸ 暂停 | 暂停当前视频（扩展不会自动续播） |
| ▶ 播放 | 手动恢复播放 |
| ⏭ 下一集 | 立即跳转到下一集 |
| 🔄 刷新列表 | 重新扫描页面中的播放列表 |

---

## 排错指南

### 现象 1：打开视频页面后，右下角没有悬浮面板

**可能原因与解决方案**：

| 原因 | 解决方案 |
|------|----------|
| 扩展未正确加载 | 回到 `chrome://extensions` 页面，确认「自动连播助手」卡片存在且「已启用」开关为打开状态 |
| 当前网站不在支持列表中 | 检查地址栏域名是否在以下列表中：`bilibili.com`、`youku.com`、`iqiyi.com`、`study.163.com`、`icourse163.org`、`zhihuishu.com`、`chaoxing.com`、`youtube.com` 等。若不在，扩展默认不激活 |
| 页面为 iframe 嵌套 | 部分网站的视频在 iframe 中，扩展仅在顶层页面创建面板。尝试点击页面中的视频区域，让焦点进入 iframe |
| 扩展冲突 | 禁用其他视频相关扩展后刷新页面重试 |

### 现象 2：面板显示了，但视频暂停后不会自动续播

**可能原因与解决方案**：

| 原因 | 解决方案 |
|------|----------|
| 视频元素未被识别 | 打开面板「日志」标签页，查看是否有 `检测到视频元素` 的记录。若无，尝试刷新页面或点击视频画面 |
| 视频为直播流 | 直播流不支持续播功能，扩展仅对点播视频生效 |
| 页面使用了 Shadow DOM | 扩展已支持 Shadow DOM，但若网站使用封闭式 Shadow DOM（closed mode），则无法穿透。这种情况无法解决 |
| 手动暂停后立即拖动进度条 | 扩展会等待 10 秒才续播，属于正常行为 |

### 现象 3：播放结束后没有自动播下一集

**可能原因与解决方案**：

| 原因 | 解决方案 |
|------|----------|
| 播放列表未识别 | 打开面板日志，查看是否有 `识别到播放列表，共 X 集` 的记录。若无，说明页面结构特殊，扩展无法自动识别 |
| 当前集为最后一集 | 播放列表最后一集播完后不会继续播放，属正常行为 |
| 播放列表在 iframe 中 | 确认扩展日志中是否有 `穿透 iframe 定位列表` 的记录。若无，尝试点击 iframe 区域后再观察 |
| 视频结束后出现推荐/广告页面 | 部分网站视频结束后会跳转到推荐页，导致扩展失去视频上下文。可在设置中开启「阻止自动跳转」选项 |

### 现象 4：面板显示异常或样式错乱

**解决方案**：

1. 刷新当前页面
2. 若问题持续，在扩展管理页面点击「自动连播助手」卡片上的 **「重新加载」** 图标（循环箭头）
3. 再次刷新视频页面

### 现象 5：扩展在部分页面不生效

**原因**：扩展默认仅在已知视频平台域名下激活，避免在无关网页上产生干扰。

**解决方案**：

- 若你的目标网站不在支持列表中，可手动编辑 `content.js` 文件中的 `KNOWN_VIDEO_HOSTS` 数组，添加目标域名
- 修改后需在 `chrome://extensions` 页面点击「重新加载」使更改生效

---

## 进阶阅读

| 资源 | 说明 |
|------|------|
| [项目 README](./README.md) | 项目概述、功能列表、技术架构说明 |
| [CHANGELOG.md](./CHANGELOG.md) | 版本更新记录与历史变更 |
| [manifest.json](./manifest.json) | 扩展配置文件详解（权限声明、脚本注入规则） |
| [content.js](./content.js) | 核心逻辑源码（视频检测、播放控制、列表识别） |
| [content.css](./content.css) | 悬浮面板样式定义（可自定义外观） |

**推荐阅读路径**：

1. 先阅读 `README.md` 了解项目全貌
2. 查看 `manifest.json` 理解扩展的权限范围和注入机制
3. 阅读 `content.js` 中 `KNOWN_VIDEO_HOSTS` 和 `init()` 函数，理解扩展的激活条件
4. 若需自定义样式，修改 `content.css` 中的 CSS 变量即可全局换色

---

> **提示**：本项目遵循 MIT 开源协议，欢迎提交 Issue 或 Pull Request 贡献代码。若遇到无法解决的问题，请在 GitHub 仓库中搜索相似 Issue 或新建 Issue 描述你的问题。